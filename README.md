Sources for the plugin `plugin.appodeal.beta.Vungle`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.Vungle" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
